ScholarNexus是一套面向复杂科研查询的智能论文搜索与推荐系统。系统将自然语言需求解析为带AND/OR/NOT语义的可执行约束图，以宽约束多源召回、双向引文扩散和跨源去重构建候选池，再通过概率校准、覆盖率估计与查询自适应集合边界平衡精确率和召回率。系统默认仅使用约两次大模型调用，高成本精判按信息价值触发；结果以高度相关、部分相关、约束满足矩阵、关系图、时间线和成本账本展示。离线回归中Macro-F1为0.5399，平均LLM调用1.86次，并提供SPAR公开评测、Docker和本地部署路径。
